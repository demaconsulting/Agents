## {PackageName}

<!-- TODO: Replace {PackageName} with the shared package name. Heading depth ## - one folder deep. -->

<!-- All sections below are MANDATORY. If a section does not apply, write
     "N/A - {justification}" rather than removing it. -->

<!-- TODO: Write a prose paragraph describing this shared package's role in the local system.
     This prose appears directly under the ## {PackageName} heading, before ### Advertised Features Consumed. -->

### Advertised Features Consumed

<!-- TODO: List which advertised features of this shared package are used by the local system.
     "Advertised features" are the capabilities the shared package itself documents as supported
     in its own repository. This repo records which of those it relies on in
     docs/reqstream/shared/{package-name}.yaml.
     - **FeatureName** — how it is used. -->

### Integration Pattern

<!-- TODO: Write a prose description of how this shared package is consumed. Cover:
     - How it is referenced (e.g. NuGet) and initialised.
     - How local systems interact with the package's public API.

     If there is a specific sequence of steps required, use a numbered list:
     1. Step one.
     2. Step two. -->

### Assumptions

<!-- TODO: Write a prose description of any assumptions the local system makes about this
     package's behaviour that are relevant to the integration design. Do not record version
     numbers — version information is managed in SBOMs outside of design documentation. -->
