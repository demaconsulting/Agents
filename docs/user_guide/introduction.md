# Introduction

<!-- TEMPLATE-DIRECTIVE:
     Replace {ProjectName} with your project name.
     IMPORTANT: This file (introduction.md) must contain ONLY Purpose, Scope, and
     References. Do not add installation steps, CLI reference, usage examples, FAQ,
     troubleshooting, or any other substantive guide content here. All such content
     belongs in separate topic files (installation.md, usage.md, faq.md, etc.) listed
     in definition.yaml. When Recreating from an old file that has extra content beyond
     these three sections, migrate that content to topic files — never discard it. -->

This guide describes how to install, configure, and use {ProjectName}.

## Purpose

<!-- TEMPLATE-DIRECTIVE:
     Describe what {ProjectName} does and why users would want it.
     Keep this to 1–3 paragraphs. Do not include installation or usage detail here. -->

## Scope

<!-- TEMPLATE-DIRECTIVE:
     Describe what this guide covers (installation, configuration, usage scenarios).
     State any prerequisites (OS, runtime, dependencies).
     List the topic files that follow this introduction (e.g. "This guide includes
     installation instructions, a full CLI reference, usage examples, and an FAQ.").
     Keep this to a short paragraph or bullet list — no subsections. -->

## References

<!-- TEMPLATE-DIRECTIVE:
     Replace this comment with references to related documents and external standards.
     - External links must be absolute URLs.
     - References to other project documents (compiled PDFs) should link to the releases
       page https://github.com/{org}/{repo}/releases — use link text that names the releases
       page, not the document (e.g. "[{ProjectName} releases](url)"), since the link goes to
       a page where the PDF is found, not directly to the PDF. Do not add any additional
       links, and never link to individual source files in docs/.
     If there are no references, replace this comment with: N/A -->
