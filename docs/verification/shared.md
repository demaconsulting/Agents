# Shared Package Verification

<!-- TEMPLATE-DIRECTIVE:
     Describe the project's overall strategy for verifying shared packages.
     The per-item files in shared/ cover the verification evidence for each individual shared package. -->

## Verification Strategy

<!-- TEMPLATE-DIRECTIVE:
     Describe how shared packages are verified in this project.
     Examples: reliance on the shared package's own CI and test suite, local integration tests
     covering the advertised features consumed by this project. -->

## Qualification Evidence

<!-- TEMPLATE-DIRECTIVE:
     What evidence is collected to demonstrate each shared package is fit for purpose?
     Examples: shared package CI results, local tests covering all consumed advertised features. -->

## Regression Approach

<!-- TEMPLATE-DIRECTIVE:
     How are shared package version upgrades handled from a verification perspective?
     Examples: re-run all local shared package tests on upgrade, coordinate with the
     shared package team to understand what changed before upgrading. -->
